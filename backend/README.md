# Backend (FastAPI)

## Run
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Env (optional)
- `DATABASE_URL`
- `S3_ENDPOINT_URL`
- `S3_BUCKET`
- `S3_ACCESS_KEY`
- `S3_SECRET_KEY`
